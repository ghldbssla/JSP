CREATE TABLE TBL_0217 (
	USERID varchar2(100) PRIMARY KEY,
	USERPW varchar2(100),
	USERNAME varchar2(100),
	USERGENDER CHAR(1),
	USERPHONE VARCHAR2(11),
	POSTCODE VARCHAR2(20),
	ADDR VARCHAR2(200),
	DTAILADDR VARCHAR2(300),
	EXTRAADDR VARCHAR2(300)
);

SELECT * FROM TBL_0217;
